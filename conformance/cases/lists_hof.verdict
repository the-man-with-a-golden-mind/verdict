module Main exposing (main)

double : Int -> Int
double n =
  n * 2

isPos : Int -> Bool
isPos n =
  n > 0

add : Int -> Int -> Int
add a b =
  a + b

main : Int
main =
  let xs = filter(isPos, [-1, 2, -3, 4]) in
  let ys = map(double, xs) in
  foldl(add, ys[0], ys)
